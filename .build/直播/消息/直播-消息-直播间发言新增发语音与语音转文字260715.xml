<title>直播-消息-直播间发言新增发语音与语音转文字260715</title>

<h1>一、需求背景</h1>

<h2>1.1 现状与问题</h2>

<p>当前直播间公聊区仅支持文本、表情包等消息形式，多语言用户之间的沟通效率有限。针对海外直播场景，部分用户更习惯通过语音表达，同时不同国家用户之间存在语言障碍，仅靠文字输入的发言门槛较高，影响直播间互动率。</p>

<p><b>需求来源：</b> <a href="https://uf1iawhques.sg.larksuite.com/wiki/T8uhwOwPliLWMVkXk1Hlz88lgKe">消息-直播间发言-新增发语音&amp;语音转文字功能</a>（产品侧）</p>

<h2>1.2 目标</h2>

<callout emoji="🎯" background-color="blue"><p><b>核心目标：</b>在直播间公聊区新增发语音及语音转文字能力，降低用户发言门槛，增强跨语言交流能力，提升直播间互动率。</p></callout>

<h2>1.3 关键指标</h2>

<table><thead><tr><th>指标</th><th>当前值</th><th>目标值</th><th>统计口径</th></tr></thead><tbody><tr><td>直播间公聊区人均发言条数</td><td>—</td><td>提升</td><td>BI</td></tr><tr><td>直播间发言用户渗透率</td><td>—</td><td>提升</td><td>BI</td></tr><tr><td>语音消息发送成功率</td><td>—</td><td>≥99%</td><td>服务端统计</td></tr></tbody></table>

<hr/>

<h1>二、需求说明</h1>

<p>本需求共 6 个功能点：</p>

<table><thead><tr><th>编号</th><th>功能点</th><th>处理端</th></tr></thead><tbody><tr><td>FUNC-001</td><td>发语音/语音转文字入口</td><td>客户端</td></tr><tr><td>FUNC-002</td><td>语音录制交互</td><td>客户端</td></tr><tr><td>FUNC-003</td><td>语音消息展示与播放</td><td>客户端+服务端</td></tr><tr><td>FUNC-004</td><td>语音消息翻译</td><td>客户端+服务端</td></tr><tr><td>FUNC-005</td><td>语音转文字</td><td>客户端+服务端</td></tr><tr><td>FUNC-006</td><td>语音内容审核</td><td>服务端</td></tr></tbody></table>

<h2>2.1 功能点 FUNC-001：发语音/语音转文字入口</h2>

<h3>功能描述</h3>

<p>在直播间聊天输入框区域新增「发语音」入口 icon。用户点击后切换至发语音操作界面；界面内提供「发语音」和「语音转文字」两个 tab，用户可自由切换。输入框入口 icon 记忆用户上一次使用的功能，下次进入直播间时默认展示对应 icon。</p>

<h3>交互规则</h3>

<ul>
<li><b>入口位置：</b> <code>[客户端]</code> 直播间聊天输入框区域，新增「发语音/语音转文字」icon；默认为「发语音」icon。</li>
<li><b>入口点击：</b> <code>[客户端]</code> 点击 icon，聊天输入框区域切换至对应的语音操作面板（发语音面板或语音转文字面板，取决于当前 icon 状态）。</li>
<li><b>tab 切换：</b> <code>[客户端]</code> 语音操作面板内包含「发语音」「语音转文字」两个 tab；点击「语音转文字」tab，面板切换至语音转文字功能界面，同时聊天输入框的「发语音」icon 切换为「语音转文字」icon；点击「发语音」tab 反之。</li>
<li><b>入口记忆：</b> <code>[客户端]</code> 入口 icon 以用户上一次使用的功能为准，本地持久化存储；用户从未使用过时默认为「发语音」icon。</li>
<li><b>面板收起：</b> <code>[客户端]</code> 点击面板外区域、发送成功、或切换至文本输入时，语音操作面板收起。</li>
</ul>

<h3>异常处理</h3>

<ul>
<li><b>数据为空：</b> <code>[客户端]</code> 无特殊空数据场景；面板首次加载失败时展示重试入口。</li>
<li><b>文本超长：</b> <code>[客户端]</code> tab 文案为固定多语言文案，按控件宽度自适应，超长时尾部截断显示省略号。</li>
<li><b>网络异常：</b> <code>[客户端]</code> 入口展示不依赖网络；无网络时仍可打开面板，录制/发送环节的网络异常见 FUNC-002。</li>
<li><b>重复操作：</b> <code>[客户端]</code> 连续快速点击入口 icon 做防抖处理，300ms 内重复点击不重复触发面板开合动画。</li>
</ul>

<h3>业务流程图</h3>

<whiteboard type="mermaid">
flowchart TD
    A[直播间聊天输入框] --&gt; B{点击语音icon}
    B --&gt;|icon为发语音| C[发语音面板]
    B --&gt;|icon为语音转文字| D[语音转文字面板]
    C --&gt;|点击语音转文字tab| D
    D --&gt;|点击发语音tab| C
    C --&gt; E[记忆本次使用功能 更新输入框icon]
    D --&gt; E
</whiteboard>

<h3>页面流程图</h3>

<p>FUNC-001 / FUNC-002 共用以下页面原型：</p>

<whiteboard/>

<h2>2.2 功能点 FUNC-002：语音录制交互</h2>

<h3>功能描述</h3>

<p>用户在发语音面板长按中心按钮录制语音，录制过程中展示动效与声音波动状态，松手发送、上滑取消。录制时长限制 1 秒至 15 秒。录制期间关闭直播间其他背景音，只收录用户麦克风声音。</p>

<h3>交互规则</h3>

<ul>
<li><b>麦克风授权：</b> <code>[客户端]</code> 首次长按录制按钮时，触发系统麦克风授权弹窗；用户拒绝授权后再次长按，引导用户前往系统设置开启麦克风权限。</li>
<li><b>开始录制：</b> <code>[客户端]</code> 长按发语音面板中心按钮，开始录制语音；按钮规律展示录制动效。</li>
<li><b>背景音处理：</b> <code>[客户端]</code> 录制期间，直播间其他背景音（主播声音、音效等）关闭，只收录用户麦克风声音；录制结束后背景音恢复原音量。</li>
<li><b>录制中提示：</b> <code>[客户端]</code> 录制期间弹出提示框，根据用户声音大小实时展示语音波动状态，并展示文案「松手发送，上滑取消」。</li>
<li><b>上滑取消：</b> <code>[客户端]</code> 录制中手指上滑至取消区域，提示文案切换为「松手取消发送」；此时松手则取消发送，录音丢弃。</li>
<li><b>松手发送：</b> <code>[客户端+服务端]</code> 录制中直接松手，客户端结束录制并将语音文件上传至服务端发送，语音操作面板收起。</li>
<li><b>时长限制：</b> <code>[客户端]</code> 录制时长最少 1 秒，最多 15 秒；录制时长不足 1 秒时松手，该语音不发送，toast 提示「说话时间过短」。</li>
<li><b>倒计时提示：</b> <code>[客户端]</code> 录制进入最后 5 秒时，提示文案切换为倒计时展示：5、4、3、2、1；达到 15 秒上限时自动结束录制并发送。</li>
</ul>

<h3>异常处理</h3>

<ul>
<li><b>数据为空：</b> <code>[客户端]</code> 录制结果为空文件（如麦克风被其他应用占用导致无声）时，不发送，toast 提示录制失败请重试。</li>
<li><b>文本超长：</b> <code>[客户端]</code> 本功能点无文本输入场景，不涉及。</li>
<li><b>网络异常：</b> <code>[客户端+服务端]</code> 语音上传失败时，toast 提示发送失败，公聊区本地消息展示失败状态并提供点击重发；重发仍使用已录制的本地语音文件。</li>
<li><b>重复操作：</b> <code>[客户端]</code> 录制结束到发送完成期间，录制按钮置为不可用状态，防止重复触发录制；同一条语音重发做防重，服务端以消息唯一 ID 去重。</li>
<li><b>录制中断：</b> <code>[客户端]</code> 录制过程中来电、切后台、直播间被关闭等中断场景，终止录制且不发送，恢复直播间背景音。</li>
</ul>

<h3>业务流程图</h3>

<whiteboard type="mermaid">
flowchart TD
    A[长按中心按钮] --&gt; B{首次使用?}
    B --&gt;|是| C[系统麦克风授权]
    C --&gt;|拒绝| D[引导去设置开启]
    C --&gt;|允许| E[开始录制]
    B --&gt;|否| E
    E --&gt; F[关闭直播间背景音 展示波动状态+提示文案]
    F --&gt; G{用户操作}
    G --&gt;|上滑| H[提示: 松手取消发送]
    H --&gt;|松手| I[取消发送 恢复背景音]
    G --&gt;|松手且时长小于1s| J[不发送 toast: 说话时间过短]
    G --&gt;|松手且时长大于等于1s| K[上传并发送语音]
    G --&gt;|达到15s上限| K
    K --&gt; L[面板收起 恢复背景音]
</whiteboard>

<h2>2.3 功能点 FUNC-003：语音消息展示与播放</h2>

<h3>功能描述</h3>

<p>语音消息发送成功后，在直播间公聊区以「语音条 + 原语种文案」两行结构展示：首行为可点击播放的语音条，第二行为语音内容识别出的原语种文字。所有直播间内用户点击语音条可播放语音，播放期间直播间背景音降低音量。</p>

<h3>交互规则</h3>

<ul>
<li><b>展示结构：</b> <code>[客户端]</code> 公聊区语音消息分两行展示——首行：语音条（含时长）；第二行：语音内容对应原语种的文字文案。</li>
<li><b>文案来源：</b> <code>[服务端]</code> 语音发送后，服务端对语音内容进行语音识别（ASR），识别出原语种文字，随消息下发给直播间内所有用户。</li>
<li><b>播放触发：</b> <code>[客户端]</code> 点击公聊区语音条，开始播放该条语音。</li>
<li><b>播放中背景音：</b> <code>[客户端]</code> 语音播放期间，直播间其他背景音降低音量；语音播放终止或播放完成后，直播间背景音量恢复。</li>
<li><b>播放终止与重播：</b> <code>[客户端]</code> 语音播放期间再次点击该语音条，播放终止；终止后再次点击语音条，从头重新播放。</li>
<li><b>互斥播放：</b> <code>[客户端]</code> 同一时间仅允许播放一条语音；播放新语音时，自动终止当前正在播放的语音。</li>
</ul>

<h3>异常处理</h3>

<ul>
<li><b>数据为空：</b> <code>[客户端+服务端]</code> 语音识别结果为空（如无有效人声）时，第二行文案不展示，仅展示语音条。</li>
<li><b>文本超长：</b> <code>[客户端]</code> 识别文案超长时，公聊区按消息气泡最大宽度自动换行完整展示，与现有文本消息规则一致。</li>
<li><b>网络异常：</b> <code>[客户端]</code> 语音文件拉取失败时，toast 提示播放失败，语音条保持可再次点击重试。</li>
<li><b>重复操作：</b> <code>[客户端]</code> 快速连续点击语音条按「播放/终止」状态切换处理，做 300ms 防抖，避免播放状态错乱。</li>
</ul>

<h3>页面流程图</h3>

<p>FUNC-003 / FUNC-004 共用以下页面原型：</p>

<whiteboard/>

<h2>2.4 功能点 FUNC-004：语音消息翻译</h2>

<h3>功能描述</h3>

<p>保留公聊区现有翻译功能，并扩展支持语音消息的识别文案。VIP 身份用户点击语音消息的原语种文案，弹出操作卡，点击「翻译」后原语种文案切换为翻译后的文案。</p>

<h3>交互规则</h3>

<ul>
<li><b>功能保留：</b> <code>[客户端+服务端]</code> 公聊区原有文本消息翻译功能保持不变。</li>
<li><b>触发条件：</b> <code>[客户端]</code> VIP 身份用户点击语音消息第二行的原语种文案，弹出操作卡（与现有文本消息操作卡一致），操作卡内包含「翻译」按钮。</li>
<li><b>权限校验：</b> <code>[客户端+服务端]</code> 客户端根据用户 VIP 身份控制操作卡翻译入口展示；服务端在翻译请求时二次校验 VIP 身份。</li>
<li><b>翻译展示：</b> <code>[客户端+服务端]</code> 点击「翻译」，服务端将原语种文案翻译为用户设置的目标语种，客户端将该条消息的原语种文案切换为翻译后文案。</li>
<li><b>非 VIP 用户：</b> <code>[客户端]</code> 非 VIP 用户点击文案不展示翻译入口，与现有文本消息翻译的身份规则保持一致。</li>
</ul>

<h3>异常处理</h3>

<ul>
<li><b>数据为空：</b> <code>[客户端]</code> 语音消息无识别文案（第二行未展示）时，无翻译入口。</li>
<li><b>文本超长：</b> <code>[客户端]</code> 翻译后文案超长时按消息气泡最大宽度自动换行完整展示。</li>
<li><b>网络异常：</b> <code>[客户端+服务端]</code> 翻译请求失败时，toast 提示翻译失败，文案保持原语种展示，用户可重新触发翻译。</li>
<li><b>重复操作：</b> <code>[客户端]</code> 翻译请求进行中，操作卡「翻译」按钮置为不可点击，防止重复请求；已翻译的消息再次点击不重复请求翻译。</li>
</ul>

<h2>2.5 功能点 FUNC-005：语音转文字</h2>

<h3>功能描述</h3>

<p>用户切换至「语音转文字」tab 后，长按中心按钮说话，语音内容实时识别为对应语种文字并自动填入聊天输入框，用户可编辑后以普通文本消息发送。</p>

<h3>交互规则</h3>

<ul>
<li><b>功能入口：</b> <code>[客户端]</code> 在语音操作面板点击「语音转文字」tab，切换至语音转文字功能界面（入口联动规则见 FUNC-001）。</li>
<li><b>麦克风授权：</b> <code>[客户端]</code> 首次长按语音转文字中心按钮时，触发系统麦克风授权弹窗；已在 FUNC-002 授权过则不重复弹出。</li>
<li><b>开始录制：</b> <code>[客户端]</code> 长按语音转文字中心按钮，开始录制语音；按钮规律展示录制动效。</li>
<li><b>背景音处理：</b> <code>[客户端]</code> 录制期间，直播间其他背景音关闭，只收录用户麦克风声音；录制结束后恢复原音量。</li>
<li><b>识别与填入：</b> <code>[客户端+服务端]</code> 语音内容识别为对应语种的文字，自动填入聊天输入框；用户可对识别结果进行编辑、删除，然后按现有文本消息流程发送。</li>
<li><b>字数上限：</b> <code>[客户端]</code> 识别文字填入后若超过输入框字数上限，截断至 100 字填入，并 toast 提示「最多输入100字」。</li>
</ul>

<h3>异常处理</h3>

<ul>
<li><b>数据为空：</b> <code>[客户端]</code> 语音未识别出有效文字时，输入框不填入内容，toast 提示未识别到内容请重试。</li>
<li><b>文本超长：</b> <code>[客户端]</code> 识别结果超过 100 字时截断填入并 toast 提示「最多输入100字」。</li>
<li><b>网络异常：</b> <code>[客户端+服务端]</code> 识别请求失败时，toast 提示识别失败请重试，输入框内容不变。</li>
<li><b>重复操作：</b> <code>[客户端]</code> 识别请求进行中，中心按钮置为不可用，防止重复触发录制。</li>
<li><b>录制中断：</b> <code>[客户端]</code> 录制过程中来电、切后台等中断场景，终止录制，已识别部分不填入输入框，恢复直播间背景音。</li>
</ul>

<h3>业务流程图</h3>

<whiteboard type="mermaid">
flowchart TD
    A[语音转文字面板] --&gt; B[长按中心按钮]
    B --&gt; C{麦克风已授权?}
    C --&gt;|否| D[系统授权弹窗]
    C --&gt;|是| E[开始录制 关闭背景音]
    D --&gt;|允许| E
    E --&gt; F[松手结束录制]
    F --&gt; G[语音识别为文字]
    G --&gt;|识别成功| H{超过100字?}
    H --&gt;|是| I[截断至100字填入 toast提示]
    H --&gt;|否| J[识别文字填入输入框]
    G --&gt;|识别失败或无内容| K[toast提示重试]
    I --&gt; L[用户编辑后按文本消息发送]
    J --&gt; L
</whiteboard>

<h3>页面流程图</h3>

<whiteboard/>

<h2>2.6 功能点 FUNC-006：语音内容审核</h2>

<h3>功能描述</h3>

<p>语音消息及其识别文案在下发到直播间前接入内容审核，拦截违规内容，保障直播间内容安全。</p>

<h3>交互规则</h3>

<ul>
<li><b>审核对象：</b> <code>[服务端]</code> 语音消息的音频文件及其 ASR 识别文案均接入内容审核；语音转文字填入输入框后发送的文本，走现有文本消息审核链路。</li>
<li><b>审核时机：</b> <code>[服务端]</code> 用户发送语音后，服务端先完成审核再下发至直播间公聊区。</li>
<li><b>审核通过：</b> <code>[服务端]</code> 审核通过的语音消息正常下发直播间内所有用户。</li>
<li><b>审核不通过：</b> <code>[客户端+服务端]</code> 审核不通过的语音消息不对其他用户下发；发送者侧的展示与提示规则与现有文本消息违规处理规则保持一致。</li>
<li><b>违规处置：</b> <code>[服务端]</code> 违规内容按平台现有违规处置规则处理（记录、警告、禁言等），与现有公聊消息处置链路一致。</li>
</ul>

<h3>异常处理</h3>

<ul>
<li><b>数据为空：</b> <code>[服务端]</code> 音频文件损坏或为空导致无法审核时，判定发送失败，客户端展示发送失败状态。</li>
<li><b>文本超长：</b> <code>[服务端]</code> 识别文案长度在 ASR 环节已受录音时长约束（最长 15 秒），无额外超长场景。</li>
<li><b>网络异常：</b> <code>[服务端]</code> 审核服务超时或异常时的兜底策略（先发后审/先审后发降级）由技术侧评估确定，需保证不阻塞直播间正常消息流。</li>
<li><b>重复操作：</b> <code>[服务端]</code> 同一条消息仅审核一次，以消息唯一 ID 去重。</li>
</ul>

<hr/>

<h1>三、设计稿</h1>

<p>设计稿完成后在此补充链接。</p>

<hr/>

<h1>四、多语言Key</h1>

<table><thead><tr><th>是否APP端</th><th>Key</th><th>中文</th></tr></thead><tbody><tr><td>是</td><td>live_voice_msg_tab_voice</td><td>发语音</td></tr><tr><td>是</td><td>live_voice_msg_tab_voice_to_text</td><td>语音转文字</td></tr><tr><td>是</td><td>live_voice_msg_release_to_send</td><td>松手发送，上滑取消</td></tr><tr><td>是</td><td>live_voice_msg_release_to_cancel</td><td>松手取消发送</td></tr><tr><td>是</td><td>live_voice_msg_too_short</td><td>说话时间过短</td></tr><tr><td>是</td><td>live_voice_msg_max_100_chars</td><td>最多输入100字</td></tr><tr><td>是</td><td>live_voice_msg_send_failed</td><td>发送失败，请重试</td></tr><tr><td>是</td><td>live_voice_msg_play_failed</td><td>播放失败，请重试</td></tr><tr><td>是</td><td>live_voice_msg_recognize_failed</td><td>未识别到内容，请重试</td></tr><tr><td>是</td><td>live_voice_msg_mic_permission_tip</td><td>请在系统设置中开启麦克风权限</td></tr></tbody></table>
