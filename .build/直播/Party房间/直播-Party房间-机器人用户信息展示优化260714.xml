<title>直播-Party房间-机器人用户信息展示优化260714</title>
<h1>一、需求背景</h1>
<h2>1.1 现状与问题</h2>
<p>Party 房间及其他直播场景中，系统会从近5天未登录的真实用户中抽取账号作为机器人填充观众列表。当用户点击这些"观众"的主页时，会看到"5天前登录"等真实登录时间，与其"正在观看直播"的在线状态明显矛盾，暴露机器人身份，影响用户对房间热度的信任感。</p>
<p><b>需求来源：</b><a href="https://uf1iawhques.sg.larksuite.com/docx/UOJldBU8jovGrDx1DATlZ4Qbgqc">需求卡片：Party房间机器人用户信息展示优化</a></p>
<h2>1.2 目标</h2>
<callout emoji="🎯" background-color="blue"><p><b>核心目标：</b>消除机器人用户信息展示中的逻辑矛盾，使虚拟观众的在线状态和登录时间展示自洽，不暴露机器人身份。</p></callout>
<h2>1.3 关键指标</h2>
<table>
<thead><tr><th>指标</th><th>当前值</th><th>目标值</th><th>统计口径</th></tr></thead>
<tbody>
<tr><td>机器人身份暴露相关用户反馈</td><td>存在</td><td>0</td><td>用户反馈/客服工单</td></tr>
</tbody>
</table>
<hr/>
<h1>二、需求说明</h1>
<h2>2.1 功能点：观众列表在线状态覆盖（FUNC-001）</h2>
<h3>功能描述</h3>
<p>用户进入 Party 房间或其他直播间，查看在线观众列表时，系统机器人用户的在线状态统一展示为"在线"，与其出现在观众列表中的身份保持一致。</p>
<h3>交互规则</h3>
<ul>
<li><b>触发条件：</b><code>[服务端]</code> 观众列表接口下发用户数据时，对当前被系统抽取为虚拟观众的用户，附加标记字段 is_virtual_audience: true</li>
<li><b>展示位置：</b><code>[客户端]</code> 观众列表中该用户的在线状态标识</li>
<li><b>展示规则：</b><code>[客户端]</code> 当用户 is_virtual_audience=true 时，在线状态强制展示为"在线"（绿色圆点），忽略真实的 last_login_time</li>
<li><b>数据来源：</b><code>[服务端]</code> 标记基于"当前该用户是否正被用作观众列表填充"的运行时状态判断，不修改用户表的真实登录时间字段</li>
<li><b>生效范围：</b><code>[客户端]</code> 仅在观众列表场景生效；其他列表（好友列表、搜索结果等）不受影响</li>
</ul>
<h3>异常处理</h3>
<ul>
<li><b>数据为空：</b><code>[客户端]</code> 若接口未返回 is_virtual_audience 字段，按 false 处理，展示真实在线状态</li>
<li><b>文本超长：</b>不涉及（在线状态为固定图标+固定文案）</li>
<li><b>网络异常：</b><code>[客户端]</code> 观众列表加载失败时展示通用加载失败提示，支持下拉重试</li>
<li><b>重复操作：</b><code>[客户端]</code> 观众列表下拉刷新做节流处理，1秒内重复下拉不重新请求</li>
</ul>
<h3>业务流程图</h3>
<whiteboard type="mermaid">
sequenceDiagram
    participant C as 客户端
    participant S as 服务端
    participant DB as 用户数据库
    C->>S: 请求观众列表
    S->>DB: 查询观众列表（含真实用户+机器人用户）
    DB-->>S: 返回用户数据
    S->>S: 对机器人用户附加 is_virtual_audience=true
    S-->>C: 返回带标记的观众列表
    C->>C: 渲染列表，机器人用户强制展示"在线"
</whiteboard>
<hr/>
<h2>2.2 功能点：个人主页登录时间覆盖（FUNC-002）</h2>
<h3>功能描述</h3>
<p>用户从观众列表点击机器人用户头像进入其个人主页时，"最近登录时间"展示为"刚刚"，在线状态展示为"在线"，与观众列表中的展示保持一致。仅从观众列表入口进入时生效，其他入口（搜索、好友列表、IM 消息等）进入同一用户主页时展示真实数据。</p>
<h3>交互规则</h3>
<ul>
<li><b>触发条件：</b><code>[客户端]</code> 从观众列表点击用户头像跳转主页时，携带来源参数 from=audience_list 及目标用户的 is_virtual_audience 标记</li>
<li><b>展示位置：</b><code>[客户端]</code> 个人主页的"最近登录时间"字段和在线状态标识</li>
<li><b>展示规则：</b><code>[客户端]</code> 当来源为观众列表（from=audience_list）且该用户 is_virtual_audience=true 时：登录时间展示为"刚刚"；在线状态展示为"在线"（绿色圆点）</li>
<li><b>非观众列表入口：</b><code>[客户端]</code> 若用户通过搜索、好友列表、IM 聊天、分享链接等其他入口进入同一用户主页，不携带观众列表来源参数，展示真实的登录时间和在线状态</li>
<li><b>数据不落地：</b><code>[客户端]</code> 覆盖逻辑仅影响当次展示，不缓存、不写入本地数据库，返回列表或切换入口后恢复真实数据</li>
</ul>
<h3>异常处理</h3>
<ul>
<li><b>数据为空：</b><code>[客户端]</code> 若主页接口返回的登录时间为空，展示为"刚刚"（与覆盖逻辑一致，无矛盾）</li>
<li><b>文本超长：</b><code>[客户端]</code> "刚刚"为固定文案，不存在超长问题；真实时间展示按现有截断规则处理</li>
<li><b>网络异常：</b><code>[客户端]</code> 主页加载失败时展示通用错误页，支持点击重试；重试时保留来源参数</li>
<li><b>重复操作：</b><code>[客户端]</code> 主页加载请求做防重处理，连续快速点击同一头像只发起一次请求</li>
<li><b>机器人用户恰好真实在线：</b><code>[客户端]</code> 此时真实数据本身就是"在线/刚刚"，覆盖逻辑等于无操作，无冲突</li>
<li><b>用户对机器人发起互动（关注/私信）：</b><code>[客户端+服务端]</code> 正常执行，不拦截。目标账号为真实用户，消息/关注关系正常生效，对方下次登录可见</li>
</ul>
<hr/>
<h1>三、设计稿</h1>
<p>设计稿完成后在此补充链接。</p>
<hr/>
<h1>四、多语言Key</h1>
<table>
<thead><tr><th>是否APP端</th><th>Key</th><th>中文</th></tr></thead>
<tbody>
<tr><td>是</td><td>user_status_online</td><td>在线</td></tr>
<tr><td>是</td><td>user_login_time_just_now</td><td>刚刚</td></tr>
</tbody>
</table>
