<title>短视频-列表-列表支持图文内容混排展示260715</title>

<h1>一、需求背景</h1>

<h2>1.1 现状与问题</h2>

<p>当前短视频列表（Feed 流）只支持展示视频类内容，运营侧沉淀的图文类内容无法在列表中露出，导致这部分内容无消费入口，内容供给的丰富度受限。</p>

<p>需要让短视频列表同时支持图文内容与视频内容的混排展示，两类内容在同一个信息流中按统一规则排列，用户在滑动浏览时可以连续消费视频和图文。</p>

<p><b>需求来源：</b> <a href="https://uf1iawhques.sg.larksuite.com/record/UtcWr8nE0e6gxmcImUklEeJcgLe">需求池记录</a></p>

<h2>1.2 目标</h2>

<callout emoji="🎯" background-color="blue"><p><b>核心目标：</b>短视频列表从「仅视频」升级为「图文+视频混排」，打通图文内容的消费入口，提升内容供给丰富度和用户浏览时长。</p></callout>

<h2>1.3 关键指标</h2>

<table>
<thead><tr><th>指标</th><th>当前值</th><th>目标值</th><th>统计口径</th></tr></thead>
<tbody>
<tr><td>列表内容曝光量</td><td>—</td><td>+X%</td><td>BI</td></tr>
<tr><td>人均浏览时长</td><td>—</td><td>+X%</td><td>BI</td></tr>
<tr><td>图文内容点击率</td><td>—</td><td>新增</td><td>BI</td></tr>
</tbody>
</table>

<hr/>

<h1>二、需求说明</h1>

<p>本次需求为纯客户端改造，不涉及后台管理端与数据统计后台。图文内容的卡片样式复用现有视频卡片样式，仅内容类型与内容详情页有差异。</p>

<h2>2.1 功能点 FUNC-001：列表支持图文内容混排展示</h2>

<h3>功能描述</h3>

<p>短视频列表接口下发的内容从「单一视频类型」扩展为「视频 + 图文」两种类型。客户端根据每条内容的类型标识，在列表中渲染对应卡片：视频类型走原有视频卡片逻辑，图文类型复用相同的卡片样式（相同的卡片尺寸、圆角、间距、标题/作者/数据展示区），仅将封面区替换为图文内容的封面图。用户在同一个列表中上下滑动时，视频卡片和图文卡片按服务端返回的顺序连续展示，无需区分入口。</p>

<h3>交互规则（每条标注处理端）</h3>

<ul>
<li><b>内容类型识别：</b> <code>[服务端]</code> 列表接口每条内容返回内容类型标识，区分「视频」与「图文」两种类型；<code>[客户端]</code> 客户端读取类型标识，决定渲染视频卡片还是图文卡片</li>
<li><b>卡片样式：</b> <code>[客户端]</code> 图文卡片完全复用视频卡片的布局规格（卡片宽高、圆角、外边距、标题行、作者头像+昵称行、点赞/浏览数据行），不新增独立样式</li>
<li><b>封面展示：</b> <code>[客户端]</code> 图文卡片封面区展示图文内容的封面图；封面图按视频卡片封面的相同裁剪规则（等比填充、居中裁剪）显示</li>
<li><b>混排顺序：</b> <code>[服务端]</code> 视频与图文在列表中的排列顺序由服务端接口统一下发，客户端严格按接口返回顺序渲染，不做二次排序</li>
<li><b>点击跳转：</b> <code>[客户端]</code> 点击视频卡片进入视频播放页（原有逻辑）；点击图文卡片进入图文内容详情页</li>
<li><b>分页加载：</b> <code>[客户端+服务端]</code> 列表下拉刷新 / 上滑加载更多沿用原有分页逻辑，服务端在每页数据中混合返回视频与图文，客户端追加渲染</li>
</ul>

<h3>异常处理</h3>

<ul>
<li><b>数据为空：</b> <code>[客户端]</code> 列表整体无数据（视频和图文均无）时，展示原有空状态占位图与文案；加载更多返回空时，展示「没有更多了」提示</li>
<li><b>封面图加载失败：</b> <code>[客户端]</code> 图文封面图加载失败时展示默认占位图（复用视频封面加载失败的占位逻辑），不留白、不塌陷卡片</li>
<li><b>文本超长：</b> <code>[客户端]</code> 图文卡片标题超出展示行数时按原有视频卡片规则截断并显示省略号，作者昵称超长同样截断</li>
<li><b>网络异常：</b> <code>[客户端]</code> 列表请求失败时展示失败提示与「点击重试」，重试触发重新拉取当前页</li>
<li><b>未知内容类型：</b> <code>[客户端]</code> 若接口返回客户端无法识别的内容类型标识（非视频、非图文），该条内容跳过不渲染，避免卡片错乱或崩溃</li>
<li><b>重复操作：</b> <code>[客户端]</code> 下拉刷新 / 加载更多做防重处理，加载中不重复触发请求；点击卡片跳转做防抖，避免连续点击打开多个页面</li>
</ul>

<h3>业务流程图</h3>

<whiteboard type="mermaid">
flowchart TD
    A[进入短视频列表] --> B[请求列表接口]
    B --> C{接口返回}
    C -->|成功且有数据| D[遍历每条内容]
    C -->|成功无数据| E[展示空状态]
    C -->|请求失败| F[展示失败提示+重试]
    D --> G{读取内容类型}
    G -->|视频| H[渲染视频卡片]
    G -->|图文| I[渲染图文卡片-复用视频样式]
    G -->|未知类型| J[跳过不渲染]
    H --> K[点击进入视频播放页]
    I --> L[点击进入图文详情页]
    F --> B
</whiteboard>

<h3>页面流程图</h3>

<whiteboard/>

<table>
<thead><tr><th>页面/状态</th><th>布局描述</th><th>关联功能</th></tr></thead>
<tbody>
<tr><td>列表-混排展示</td><td>视频卡片与图文卡片按接口顺序上下混排，卡片样式统一</td><td>FUNC-001</td></tr>
<tr><td>列表-图文卡片</td><td>复用视频卡片布局，封面区为图文封面图</td><td>FUNC-001</td></tr>
<tr><td>列表-空状态</td><td>无内容时的占位图与提示文案</td><td>FUNC-001</td></tr>
<tr><td>图文详情页</td><td>点击图文卡片后进入的内容详情页</td><td>FUNC-001</td></tr>
</tbody>
</table>

<hr/>

<h1>三、设计稿</h1>

<callout emoji="📝" background-color="grey"><p>设计稿完成后在此补充链接。</p></callout>

<hr/>

<h1>四、多语言Key</h1>

<table>
<thead><tr><th>是否APP端</th><th>Key</th><th>中文</th></tr></thead>
<tbody>
<tr><td>是</td><td>list_empty_no_content</td><td>暂无内容</td></tr>
<tr><td>是</td><td>list_no_more</td><td>没有更多了</td></tr>
<tr><td>是</td><td>list_load_failed_retry</td><td>加载失败，点击重试</td></tr>
</tbody>
</table>
